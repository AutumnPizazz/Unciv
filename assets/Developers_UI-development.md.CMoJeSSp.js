import{_ as t,o as a,c as o,ag as s}from"./chunks/framework.D9ghT4bC.js";const i="/Unciv/assets/FasterUIDevelopment.mrjkBH_L.png",g=JSON.parse('{"title":"UI Development","description":"","frontmatter":{},"headers":[],"relativePath":"Developers/UI-development.md","filePath":"Developers/UI-development.md"}'),n={name:"Developers/UI-development.md"};function l(r,e,d,c,h,p){return a(),o("div",null,[...e[0]||(e[0]=[s('<h1 id="ui-development" tabindex="-1">UI Development <a class="header-anchor" href="#ui-development" aria-label="Permalink to &quot;UI Development&quot;">​</a></h1><p>Unciv is backed by <a href="https://libgdx.com/wiki/graphics/2d/scene2d/scene2d" target="_blank" rel="noreferrer">GDX&#39;s scene2d</a> for the UI, so check out <a href="https://libgdx.com/wiki/graphics/2d/scene2d/scene2d" target="_blank" rel="noreferrer">their official documentation</a> for more info about that.</p><p>We mainly use the <a href="https://libgdx.com/wiki/graphics/2d/scene2d/table" target="_blank" rel="noreferrer"><code>Table</code> class</a> of scene2d, because it offers nice flexibility in laying out all the user interface.</p><h2 id="the-fasteruidevelopment-class" tabindex="-1">The <code>FasterUIDevelopment</code> class <a class="header-anchor" href="#the-fasteruidevelopment-class" aria-label="Permalink to &quot;The `FasterUIDevelopment` class&quot;">​</a></h2><p>This class is basically just a small helper GDX application to help develop UI components faster.</p><p>It sets up the very basics of Unciv, so that you can then show one single UI component instantly. This gives you much faster response times for when you change something, so that you can immediately see the changes you made, without having to restart the game, load a bunch of stuff and navigate to where your UI component would actually be.</p><p>To use it, you set up a &#39;provider&#39; that delivers an Actor to test, an instance of the UI element you want to develop, potentially with mock content to make it self-contained. This provider can be any class that implements the <code>IFasterUITester</code> interface, and it needs to be made available at the beginning of the [FasterUIDevelopment] source. The default way to do so is to add another entry to the <code>FasterUIDevTesters</code> enum. (It&#39;s possible to keep the test setup code closer to the code to test, in the core module, all you have to do is refactor the <code>IFasterUITester</code> definition to the core module. <em>Don&#39;t</em> add a dependency on the test module to core instead.)</p><p>Example - works when inserted just before the &quot;<code>;</code>&quot; in <code>FasterUIDevTesters</code>:</p><div class="language-kotlin vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">kotlin</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">    MyTester</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>\n<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">        override</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> fun</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> testCreateExample</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(screen: </span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">BaseScreen</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Label</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;My testing Label!&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, BaseScreen.skin)</span></span>\n<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    },</span></span></code></pre></div><p>Note you can disable a tester by overriding <code>testGetLabel</code> to return <code>null</code>.</p><p>When you launch <code>FasterUIDevelopment</code> as application (see below), you will be presented with a list of buttons to choose a test (unless only one test is enabled). Here you can also enable <a href="#the-gdx-scene2d-debug-option">Scene2D debugging</a> if needed. Once a specific test is up and running, your UI element is centered in the window and gets an orange border (the border actors are named, so you can easily recognize them in Scene2D debug). Should your Actor not have sized itself (e.g. by <code>pack()</code>ing a <code>Table</code> that contains elements reporting sizes), it will be automatically sized to 90% of the stage. You can still toggle Scene2D debugging with the middle mouse button (click the wheel). You can leave any screen using the ESC key. Resizing the window on the testing screen is not supported, but you can do so on the test chooser. A resize is not prevented, but no guarantees can be given for the results.</p><p>There&#39;s two ways to launch <code>FasterUIDevelopment</code>:</p><ul><li>Open this file in Android Studio under project:docs/Developers. That line above has a little green arrow in the left gutter that can be clicked.</li><li>Create a Run Configuration similar to <a href="./Building-Locally.html#with-android-studio">the desktop one described here</a>, but with classpath <code>Unciv.tests.test</code> and main class <code>com.unciv.dev.FasterUIDevelopment</code>. <img src="'+i+'" alt="image"> This way you can set breakpoints and debug.</li></ul><h2 id="the-gdx-scene2d-debug-option" tabindex="-1">The &#39;Gdx Scene2D debug&#39; option <a class="header-anchor" href="#the-gdx-scene2d-debug-option" aria-label="Permalink to &quot;The &#39;Gdx Scene2D debug&#39; option&quot;">​</a></h2><p>This option can be toggled on the secret &#39;Debug&#39; page within Unciv&#39;s Options, or as described above in <a href="#the-fasteruidevelopment-class">FasterUIDevelopment</a>.</p><p>It turns on several UI debug features:</p><ul><li>Gdx Actor debug lines</li><li>Mouse coordinates and FPS</li><li>Descriptor of the Actor under the mouse</li><li>Coordinate scales</li><li>Logging touchDown on the stage level, before Scene2D gets to process the event</li></ul><h3 id="gdx-actor-debug-lines" tabindex="-1">Gdx Actor debug lines <a class="header-anchor" href="#gdx-actor-debug-lines" aria-label="Permalink to &quot;Gdx Actor debug lines&quot;">​</a></h3><p>See <a href="https://libgdx.com/wiki/graphics/2d/scene2d/table#logical-table" target="_blank" rel="noreferrer">Gdx wiki</a> The Scene2D debug option turns on mouse-over Gdx debug lines at the stage level using <code>setDebugUnderMouse</code>, <code>setDebugTableUnderMouse</code> and <code>setDebugParentUnderMouse</code>.</p><ul><li>Blue lines are Table bounds - Each Table has dimensions as a Widget and as a logical table determined by the cells. They will coincide if there is both expandX and expandY set somewhere.</li><li>Red lines are Cell bounds - the part within cell padding.</li><li>Green lines are Actor bounds of the Cell contents. If the Cell has both Fill directions set, they disappear below the red cell bounds (that is, unless the content Actor has a maxSize limiting the fill).</li></ul><h3 id="mouse-coordinates-and-fps" tabindex="-1">Mouse coordinates and FPS <a class="header-anchor" href="#mouse-coordinates-and-fps" aria-label="Permalink to &quot;Mouse coordinates and FPS&quot;">​</a></h3><p>On the bottom right is a semi-transparent panel with 3 numbers on top: X and Y of the mouse in Gdx stage coordinates, and FPS.</p><h3 id="descriptor-of-the-actor-under-the-mouse" tabindex="-1">Descriptor of the Actor under the mouse <a class="header-anchor" href="#descriptor-of-the-actor-under-the-mouse" aria-label="Permalink to &quot;Descriptor of the Actor under the mouse&quot;">​</a></h3><p>The lower part of said panel shows a string helping to identify which Actor the mouse is over. This will look for the actor&#39;s parent and potentially children, and the optional Actor.name field. Java class names or Label text are used when appropriate - it tries to build something short, but just descriptive enough. It uses the following separators / symbols:</p><ul><li><code>:</code> a colon separates class name from Actor.name - or Actor.name is used undecorated if it contains the class name.</li><li><code>&quot;</code> double-quotes show actual Label or TextButton text, max 20 characters, prefixed directly with the class name.</li><li><code>.</code> a dot separates parent from hit Actor: If the above alone does not yield a descriptive label, the parent (if any) is added as descriptive label per the rules above or as simple class name.</li><li><code>(</code>..<code>)</code> after the above designates a sample from the children (the first nicely descriptive one), only if the parent won&#39;t add good recognition value.</li></ul><h3 id="coordinate-scales" tabindex="-1">Coordinate scales <a class="header-anchor" href="#coordinate-scales" aria-label="Permalink to &quot;Coordinate scales&quot;">​</a></h3><p>All four edges of the screen get tiny tick marks, each 20 units in the Gdx stage coordinates, to help estimate paddings or sizes.</p><h3 id="click-logging" tabindex="-1">Click logging <a class="header-anchor" href="#click-logging" aria-label="Permalink to &quot;Click logging&quot;">​</a></h3><p>Click logging happens through the regular logging system with the tag <code>[StageMouseOverDebug]</code>, which is excluded by default. So, to see these events, you must have your run environment set up for logging and enable that tag. A line contains the screen coordinates (in pixels, y goes top-down), the pointer number (e.g. when someone has a trackpad and mouse), the button number, and the &quot;descriptor&quot; as described above for the &quot;<code>hit</code>&quot; actor. This hit test is performed with <code>touchable = true</code>, which can be different from the hit test used to show which actor the mouse is over.</p>',29)])])}const b=t(n,[["render",l]]);export{g as __pageData,b as default};
