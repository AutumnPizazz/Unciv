import{_ as i,o,c as t,ag as a}from"./chunks/framework.D9ghT4bC.js";const g=JSON.parse('{"title":"Building for and testing on Android","description":"","frontmatter":{},"headers":[],"relativePath":"Developers/Testing-Android-Builds.md","filePath":"Developers/Testing-Android-Builds.md"}'),n={name:"Developers/Testing-Android-Builds.md"};function l(d,e,s,r,u,c){return o(),t("div",null,[...e[0]||(e[0]=[a('<h1 id="building-for-and-testing-on-android" tabindex="-1">Building for and testing on Android <a class="header-anchor" href="#building-for-and-testing-on-android" aria-label="Permalink to &quot;Building for and testing on Android&quot;">​</a></h1><p>This is a work in progress - feel free to contribute. Much of this information is not specific to Unciv and publicly available.</p><h2 id="run-configuration" tabindex="-1">Run configuration <a class="header-anchor" href="#run-configuration" aria-label="Permalink to &quot;Run configuration&quot;">​</a></h2><p>A successful Gradle sync should automatically create a Run configuration for &quot;android&quot;. If not, you might try creating one yourself (however, if the config is missing due to an incomplete Gradle sync, then you likely won&#39;t be able to choose the module):</p><ul><li>In Android Studio, Run &gt; Edit configurations (be sure the Gradle sync is finished successfully first). <ul><li>Click &quot;+&quot; to add a new configuration</li><li>Choose &quot;Android App&quot;</li><li>Give the configuration a name, we recommend &quot;Android&quot;</li><li>Set module to <code>Unciv.android.main</code></li><li>On the Miscellaneous tab, we recommend checking both logcat options</li><li>On the Debugger tab, we recommend checking <code>Automatically attach on Debug.waitForDebugger()</code></li><li>That&#39;s it, the rest can be left as defaults.</li></ul></li></ul><h2 id="physical-devices" tabindex="-1">Physical devices <a class="header-anchor" href="#physical-devices" aria-label="Permalink to &quot;Physical devices&quot;">​</a></h2><p>Debugging on physical devices is actually easiest. With Studio running, you will have adb running, and any newly connected device that generally allows debugging will ask to confirm your desktop&#39;s fingerprint (use an USB cable for this tutorial, IP is another matter). Once adb sees the device and your desktop is authorized from the device, it will be available and preselected on the device select-box to the right of your &quot;android&quot; run configuration and you can start debugging just like the desktop version. <strong>Note</strong> A debug session does not end after selecting Exit from Unciv&#39;s menus - swipe it out of the recents list to end the debug session. Hitting the stop button in Studio is less recommended. That&#39;s an Android feature.</p><h3 id="adb-on-linux" tabindex="-1"><code>adb</code> on Linux <a class="header-anchor" href="#adb-on-linux" aria-label="Permalink to &quot;`adb` on Linux&quot;">​</a></h3><p>For the above (using USB) to succeed on Linux, you may need to prepare permissions. The exact requirements depend on the distribution - if in doubt, search online resources. For a Mint 22.1 distro it&#39;s as follows, but the steps should be mappable to other distros easily:</p><ul><li>Ensure you&#39;re a member of group <code>plugdev</code> (bash: <code>groups</code>). It&#39;s default on Mint, but if not, add yourself (<code>sudo usermod -a -G plugdev $USER</code>).</li><li>Ensure a matching udev rule exists. This can depend on the maker of the device you wish to connect. To find the maker ID, while the device is connected: <code>lsusb</code>. Find your device and jot down the first 4-digit hex code after &quot;ID&quot;. In many cases that will be <code>18d1</code> - e.g. LineageOS installs will mostly pretend to be 18d1, even if it&#39;s different hardware.</li><li>Look for existing rules in <code>/etc/udev/rules.d/</code>. A typical name would be <code>51-android.rules</code>. Those are text files, and all are processed, so guess from their names and if in doubt check them all.</li><li>Edit or create the rules file. On Mint 22+, using xed under sudo is fine, otherwise you might prefer to use another editor like nano. <code>sudo xed /etc/udev/rules.d/51-android.rules</code></li><li>In the typical case, the file would have one line: <code>SUBSYSTEM==&quot;usb&quot;, ATTR{idVendor}==&quot;18d1&quot;, MODE=&quot;0666&quot;, GROUP=&quot;plugdev&quot;</code>. Yes, replace that <code>18d1</code> with your maker ID. If the file and any lines for different maker IDs already exist, add another line.</li><li>Save the file ~and reboot.~ Just joking - we&#39;re on Linux, so a <code>sudo udevadm control --reload-rules &amp;&amp; sudo udevadm trigger</code> will do.</li><li>Try <code>adb kill-server &amp;&amp; adb devices</code>: Your device should be in the list, and Studio will be able to talk to it.</li><li>You will likely have to re-authorize your computer from the device. Make sure no notification drawer or other system UI is covering and hiding the prompt.</li><li>If you still have problems, make sure no gradle daemons are still running under the old permissions: <code>./gradlew --status</code>. If there are, kill them: <code>./gradlew --stop</code> (or skip the status right away).</li></ul><h2 id="building-an-apk" tabindex="-1">Building an APK <a class="header-anchor" href="#building-an-apk" aria-label="Permalink to &quot;Building an APK&quot;">​</a></h2><p>Android Studio has a menu entry &quot;Build -&gt; Build Bundle(s) / APK(s) -&gt; Build APK(s).&quot; This will build a ready-to-install APK, and when it is finished, pop a message that offers to show you the file in your local file manager. <em><strong>Important</strong></em> such locally built APK&#39;s are debug-signed and not interchangeable with Unciv downloaded from stores. You cannot update one with the other or switch without uninstalling first - losing all data. The command line equivalent is: <code>./gradlew android:assembleDebug</code>. Then find the apk yourself under <code>android/build/outputs/apk/debug</code>.</p><h2 id="virtual-devices-avd" tabindex="-1">Virtual devices (AVD) <a class="header-anchor" href="#virtual-devices-avd" aria-label="Permalink to &quot;Virtual devices (AVD)&quot;">​</a></h2><p>(TODO)</p><ul><li>Install Emulator</li><li>Intel HAXM: Deprecated by Intel but still recommended</li><li>Download system image <ul><li>Choice: Match host architecture, w/o Google, older is faster...?</li></ul></li><li>Configure AVD</li><li>Debug on AVD</li></ul><h2 id="unciv-s-log-output" tabindex="-1">Unciv&#39;s log output <a class="header-anchor" href="#unciv-s-log-output" aria-label="Permalink to &quot;Unciv&#39;s log output&quot;">​</a></h2><p>Unciv&#39;s log system runs on top of the Android SDK one, and filters and tags the messages before passing them to the system.</p><p>Like the desktop variant, it has a &#39;release&#39; mode where all logging from Unciv code is dropped. A release is detected when the actual APK manifest says debuggable=false - all possibilities discussed here are debug builds in that sense. Running from Studio it does not matter which button you use - Run or Debug - both deploy a debug build, the difference is only whether it attaches the debugger right away. An APK built from Studio is also always a debug build.</p><p>Therefore, logging is always enabled unless you run a store version. You can override this by providing an intent extra: In your Run configuration, on the &quot;General&quot; Tab, add in the &quot;Launch Flags&quot; field: <code>--ez debugLogging false</code>. The override can also be controlled without Studio using the activity manager:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>adb shell am start com.unciv.app/com.unciv.app.AndroidLauncher --ez debugLogging true</span></span></code></pre></div><p>(or <code>am start...</code> directly from a device terminal) will turn on logging for a release (store) build.</p><p>The log system&#39;s filtering capabilities that work by providing <code>-D</code> options to the Java virtual machine cannot be controlled on Android as far as we know. (TODO - document those in the desktop/Studio wiki article)</p><h2 id="reading-the-logcat" tabindex="-1">Reading the logcat <a class="header-anchor" href="#reading-the-logcat" aria-label="Permalink to &quot;Reading the logcat&quot;">​</a></h2><p>(TODO)</p><ul><li>Studio <ul><li>If the logcat window is missing: View - Tool Windows - Logcat</li></ul></li><li>Studio&#39;s filtering <ul><li>When you debug Unciv, a matching filter is pre-applied to the Logcat window, but the tool can actually show the entire system log, including those for other apps.</li><li>Using <code>package:com.unciv.app tag:Unciv</code> as filter is useful to see only the output of Unciv&#39;s own logging system.</li></ul></li><li>logcat apps on the device <ul><li><code>com.pluscubed.matloglibre</code>? Outdated.</li></ul></li><li>logcat apps need root or specific authorization <ul><li><code>adb shell pm grant &lt;logcat app&#39;s package id&gt; android.permission.READ_LOGS</code></li></ul></li></ul>',25)])])}const p=i(n,[["render",l]]);export{g as __pageData,p as default};
