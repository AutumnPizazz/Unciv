import{_ as t,o as a,c as o,ag as n}from"./chunks/framework.D9ghT4bC.js";const m=JSON.parse('{"title":"Translation generation - for developers","description":"","frontmatter":{},"headers":[],"relativePath":"Translating/Translation-generation.md","filePath":"Translating/Translation-generation.md"}'),s={name:"Translating/Translation-generation.md"};function r(l,e,i,d,u,h){return a(),o("div",null,[...e[0]||(e[0]=[n('<h1 id="translation-generation-for-developers" tabindex="-1">Translation generation - for developers <a class="header-anchor" href="#translation-generation-for-developers" aria-label="Permalink to &quot;Translation generation - for developers&quot;">​</a></h1><h2 id="the-automatic-template-generation" tabindex="-1">The automatic template generation <a class="header-anchor" href="#the-automatic-template-generation" aria-label="Permalink to &quot;The automatic template generation&quot;">​</a></h2><p>Before releasing every version, we regenerate the translation files.</p><p>Sometimes, new strings (names, uniques, etc) are added in the json files. In order to not have to add every single one to the translation files manually, we have a class - TranslationFileWriter - that, for every language:</p><ul><li>Goes over the template.properties and copies translation lines</li><li>For every json file in the jsons folder <ul><li>Selects all string values - both in objects, and in arrays in objects, to any inheritance or nesting level. (Collections that can be parsed must be derived from List or AbstractCollection)</li><li>Generates a &#39;key = value&#39; line</li></ul></li><li>Scans knowledge from UniqueType and UniqueParameterType instances and generates &#39;key = value&#39; lines for them</li></ul><p>This means that every text that ISN&#39;T in the jsons or the UniqueType system needs to be added manually to the template.properties in order to be translated! That also means if you&#39;ve been adding new json structures you (or someone) should check TranslationFileWriter and see if it is able to cope with them.</p><h2 id="rules-for-templates-added-manually" tabindex="-1">Rules for templates added manually <a class="header-anchor" href="#rules-for-templates-added-manually" aria-label="Permalink to &quot;Rules for templates added manually&quot;">​</a></h2><p>Building a new UI and doing something like <code>popup.add(&quot;Hello world&quot;.toLabel())</code> is a typical case: This is not contained in json data, so you&#39;ll have to add the template to <code>template.properties</code> yourself. For this example, adding <code>Hello world = </code> somewhere in a line of its own could suffice.</p><p>Note the space at the end - it&#39;s absolutely required, and see to it your editor does not destroy your work. If you want to make sure, use Android Studio for git integration, but edit the file in an external editor, then run the unit tests locally before pushing. (to do: add link for instructions how to do that)</p><p>Leading spaces on a translation line or more than one space between the text and the <code>=</code> would mean these spaces are a <em>key part of the string to be translated</em>. That can work, but be warned: translators often overlook that those spaces are a required part of <em>both</em> template <em>and</em> translation, so if you <em>can</em> do without, then doing without is safer.</p><p>Translation templates can use placeholders, and there&#39;s two varieties: <code>[]</code> and <code>{}</code>. Square ones take precedence over curly ones, and nesting works only with a single level of curly nested inside one level of square. In both cases the symbols themselves (<code>[]{}</code>) are removed by the translation engine.</p><p>Square brackets <code>[]</code> mean the outer and inner components are both translated individually. The outer template will use alias names inside the brackets - example: Your code outputs &quot;Everyone gains [5000] gold!&quot;, then the translation template should be &quot;Everyone gains [amount] gold! = &quot;. The translation engine would translate the &quot;Everyone gains [] gold!&quot; and &quot;5000&quot; individually and reassemble them - of course, the number is simply passed through in most cases (unless the locale from the LocaleCode enum defines otherwise). But in other cases that could be e.g. a Unit name that would be translated, and you could trust that translations for units are already handled just fine. Note that <a href="./../Modders/uniques.html">uniques</a> often use the feature, but it is in no way limited to them. If it makes life easier for translators, use it.</p><p>Curly brackets <code>{}</code> are simpler - the contents within the brackets are translated individually, while the outer parts are passed through verbatim. Example: <code>&quot;+$amount${Fonts.gold} {Gold}&quot;.toLabel()</code> - note the first <code>${}</code> is a kotlin template while the second pair becomes part of the string. It tells the translation engine to ignore the numbers and the symbol but to translate the single word &quot;Gold&quot;.</p><h2 id="rules-for-all-sources" tabindex="-1">Rules for all sources <a class="header-anchor" href="#rules-for-all-sources" aria-label="Permalink to &quot;Rules for all sources&quot;">​</a></h2><p>The [], {} and &lt;&gt; bracket types are used internally and cannot be part of a translatable text. Use () instead.</p>',15)])])}const p=t(s,[["render",r]]);export{m as __pageData,p as default};
