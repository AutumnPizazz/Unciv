import{_ as t,o as a,c as i,ag as n}from"./chunks/framework.D9ghT4bC.js";const m=JSON.parse('{"title":"Project structure","description":"","frontmatter":{},"headers":[],"relativePath":"Developers/Project-structure-and-major-classes.md","filePath":"Developers/Project-structure-and-major-classes.md"}'),o={name:"Developers/Project-structure-and-major-classes.md"};function r(s,e,l,c,h,d){return a(),i("div",null,[...e[0]||(e[0]=[n('<h1 id="project-structure" tabindex="-1">Project structure <a class="header-anchor" href="#project-structure" aria-label="Permalink to &quot;Project structure&quot;">​</a></h1><p>Since LibGDX, and therefore Unciv, are built for multi-platform support, the project structure is built accordingly.</p><p>99% of the code is in the <a href="https://github.com/yairm210/Unciv/tree/master/core" target="_blank" rel="noreferrer">core</a> project, which contains all the platform-independent code.</p><p>The <a href="https://github.com/yairm210/Unciv/tree/master/desktop" target="_blank" rel="noreferrer">desktop</a> and <a href="https://github.com/yairm210/Unciv/tree/master/android" target="_blank" rel="noreferrer">android</a> folders contain platform-specific things, and the Android folder also contains the game Images and the all-important Assets, which are required for running from Desktop as well, so we bundle them up into the .jar file when releasing.</p><p>The <a href="https://github.com/yairm210/Unciv/tree/master/tests" target="_blank" rel="noreferrer">tests</a> folder contains tests that can be run manually via gradle with <code>./gradlew tests:test</code>, and are run automatically by Travis for every push.</p><p>The <a href="https://github.com/yairm210/Unciv/tree/master/server" target="_blank" rel="noreferrer">server</a> folder contains the sources for the UncivServer (a host enabling communication between multiplayer game instances), which is packaged into its own separate jar.</p><h2 id="translations" tabindex="-1">Translations <a class="header-anchor" href="#translations" aria-label="Permalink to &quot;Translations&quot;">​</a></h2><p>Before we get to the Classes, a word on Languages. Unciv is playable in several handfuls of languages, and there&#39;s magic to support that. Whenever you include a new string in code you will need to give it a quick evaluation - will users see it, and if so, what do I need to do to support its translations. Sometimes you may not need to do anything, sometimes you will add a line to the <a href="https://github.com/yairm210/Unciv/blob/master/android/assets/jsons/translations/template.properties" target="_blank" rel="noreferrer">translation templates</a>, and sometimes you will adapt the string formatting to support the translations. For details, see <a href="./../Translating/Translation-generation.html">the &#39;Translation generation - for developers&#39; chapter</a>.</p><h2 id="major-classes" tabindex="-1">Major classes <a class="header-anchor" href="#major-classes" aria-label="Permalink to &quot;Major classes&quot;">​</a></h2><p>Civ, and therefore Unciv, is a game with endless interconnectivity - everything affects everything else.</p><p>In order to have some semblance of order, we&#39;ll go over the main classes in the order in which they are serialized.</p><p>So yes, you can - for instance - get the center tile of a city, a TileInfo, directly from CityInfo. But delving into all the connections would only harm the point of this overview, that&#39;s what the actual code is for 😉</p><p>The Game State:</p><ul><li>GameInfo <ul><li>CivilizationInfo <ul><li>CityInfo</li></ul></li><li>TileMap <ul><li>TileInfo <ul><li>MapUnit</li></ul></li></ul></li><li>RuleSet (unique in that it is not part of the game state)</li></ul></li></ul><p>The UI:</p><ul><li>MainMenuScreen</li><li>NewGameScreen</li><li>WorldScreen</li><li>CityScreen</li><li>MapEditorScreen</li><li>Picker Screens - TechPickerScreen, PolicyPickerScreen, ImprovementPickerScreen, PromotionPickerScreen</li></ul><h2 id="game-state" tabindex="-1">Game State <a class="header-anchor" href="#game-state" aria-label="Permalink to &quot;Game State&quot;">​</a></h2><h3 id="the-game-gameinfo" tabindex="-1">The Game - <code>GameInfo</code> <a class="header-anchor" href="#the-game-gameinfo" aria-label="Permalink to &quot;The Game - `GameInfo`&quot;">​</a></h3><p>First off, let&#39;s clarify: When we say &quot;The Game&quot;, we mean the <em>state</em> of the game (what turn it is, who the players are, what each one has etc) and not the <em>UI</em> of the game.</p><p>That is, The Game is the <em>currently played</em> game, not <em>Unciv</em>.</p><p>The game contains three major parts:</p><ul><li>The list of the players, or civilizations - <code>List&lt;CivilizationInfo&gt;</code></li><li>The map upon which the game is played - <code>TileMap</code></li><li>The ruleset by which the game is played - <code>RuleSet</code>. This includes what technologies, buildings, units etc. are available, and IS NOT serialized and deserialized, but comes straight from the game files - more on that later.</li><li>Parameters unique to this game - difficulty, game speed, victory conditions, etc.</li></ul><p>When we save the game, or load the game, we&#39;re actually serializing and deserializing this class, which means that the this class is the root of the entire game state.</p><p>Most objects in the &quot;state tree&quot; have a transient reference to their parent, meaning the tree can be traversed in-code in all directions, and frequently is.</p><h3 id="a-civilization-civilizationinfo" tabindex="-1">A Civilization - <code>CivilizationInfo</code> <a class="header-anchor" href="#a-civilization-civilizationinfo" aria-label="Permalink to &quot;A Civilization - `CivilizationInfo`&quot;">​</a></h3><p>This represents one of the players of the game, and NOT a specific nation - meaning, not France, but rather &quot;Player X who is France in this game&quot;. In another game, there will be another France.</p><p>As one of the focal points of the game, it contains a lot of important information, the most important of which are:</p><ul><li>The list of cities the civilization has - <code>List&lt;CityInfo&gt;</code></li><li>Which nation this is - references a certain Nation (part of the ruleset)</li><li>Various Managers for the different aspects of the civilization - <code>PolicyManager</code>, <code>GoldenAgeManager</code>, <code>GreatPersonManager</code>, <code>TechManager</code>, <code>VictoryManager</code>, <code>DiplomacyManager</code></li></ul><h3 id="a-city-cityinfo" tabindex="-1">A City - <code>CityInfo</code> <a class="header-anchor" href="#a-city-cityinfo" aria-label="Permalink to &quot;A City - `CityInfo`&quot;">​</a></h3><p>This contains the information about a specific city.</p><p>Beyond basic information like name, location on map etc, the most important classes it contains are:</p><ul><li>Calculating the yield of the city - <code>CityStats</code></li><li>Managers for the various aspects - <code>PopulationManager</code>, <code>CityConstructions</code>, <code>CityExpansionManager</code></li><li>The tiles controlled and worked by the city - only their locations are permanently saved in the CityInfo, the actual information is in the TileInfo in the TileMap</li></ul><h3 id="the-map-tilemap" tabindex="-1">The map - <code>TileMap</code> <a class="header-anchor" href="#the-map-tilemap" aria-label="Permalink to &quot;The map - `TileMap`&quot;">​</a></h3><p>This contains mostly helper functions and acts as a wrapper for the list of tiles it contains</p><h3 id="a-tile-tileinfo" tabindex="-1">A tile - <code>TileInfo</code> <a class="header-anchor" href="#a-tile-tileinfo" aria-label="Permalink to &quot;A tile - `TileInfo`&quot;">​</a></h3><p>Each tile is comprised of several layers, and so has information for each.</p><p>Tiles have, primarily:</p><ul><li>A base terrain - Grassland, Hills, Desert etc. References a certain <code>Terrain</code> (part of the ruleset)</li><li>An optional terrain feature - Forest, Jungle, Oasis etc. References a certain <code>Terrain</code> (part of the ruleset)</li><li>An optional resource - Iron, Dye, Wheat etc. References a certain <code>TileResource</code> (part of the ruleset)</li><li>An improvement built on the tile, if any. References a certain <code>TileImprovement</code> (part of the ruleset)</li><li>The units that are currently in the tile - <code>MapUnit</code></li></ul><h3 id="a-unit-on-the-map-mapunit" tabindex="-1">A unit on the map - <code>MapUnit</code> <a class="header-anchor" href="#a-unit-on-the-map-mapunit" aria-label="Permalink to &quot;A unit on the map - `MapUnit`&quot;">​</a></h3><p>Unlike buildings, Unit in Unciv has two meanings. One is a <em>Type</em> of unit (like Spearman), and one is a specific instance of a unit (say, a Babylonian Spearman, at a certain position, with X health).</p><p><code>MapUnit</code> is a specific instance of a unit, whereas <code>BaseUnit</code> is the type of unit.</p><p>Main information:</p><ul><li>A name - references a specific <code>BaseUnit</code></li><li>Health and Movement</li><li>Promotion status - <code>UnitPromotions</code></li></ul><h3 id="ruleset" tabindex="-1">Ruleset <a class="header-anchor" href="#ruleset" aria-label="Permalink to &quot;Ruleset&quot;">​</a></h3><p>So far so good - but what of everything that makes Civ, Civ? The units, the buildings, the nations, the improvements etc?</p><p>Since these things remain the same for every game, these are not saved on a per-game basis, but rather are saved in json files in Unciv&#39;s asset folder.</p><p>Each class in the game state that saves one of these will reference it by name, and when the game is running it will check the Ruleset to find the relevant information for that object.</p><p>The various objects are:</p><ul><li><code>Technology</code> - referenced mainly in <code>CivilizationInfo.TechManager</code></li><li><code>Nations</code> - referenced mainly in <code>CivilizationInfo</code></li><li><code>Policy</code> - referenced mainly in <code>CivilizationInfo.PolicyManager</code> (seeing a pattern here?)</li><li><code>Building</code> - referenced mainly in <code>CityInfo.ConstructionManager</code></li><li><code>BaseUnit</code> - referenced mainly in <code>MapUnit</code></li><li><code>Promotion</code> - referenced mainly in <code>MapUnit</code></li><li><code>Terrain</code> - referenced mainly in <code>TileInfo</code></li><li><code>TileResource</code> - referenced mainly in <code>TileInfo</code></li><li><code>TileImprovement</code> - referenced mainly in <code>TileInfo</code></li></ul><p>There are also Translations in the Ruleset, but they technically have nothing to do with the game state but rather with the UI display.</p><p>The information for all of these is in json files in <code>android\\assets\\jsons</code></p><h2 id="ui" tabindex="-1">UI <a class="header-anchor" href="#ui" aria-label="Permalink to &quot;UI&quot;">​</a></h2><p><code>UncivGame</code> is the &#39;base&#39; class for the UI, from which everything starts, but it itself doesn&#39;t do much.</p><p>When we change a screen, we&#39;re changing a value in UncivGame, the interesting stuff happens in the screens themselves.</p><h3 id="the-main-menu-mainmenuscreen" tabindex="-1">The main menu - <code>MainMenuScreen</code> <a class="header-anchor" href="#the-main-menu-mainmenuscreen" aria-label="Permalink to &quot;The main menu - `MainMenuScreen`&quot;">​</a></h3><p>This is what the user sees when first entering the game. It acts as a hub to loading games, adding mods, options etc, without loading an actual game upfront - this allows us to differentiate between &quot;User can&#39;t enter game&quot; and &quot;User can&#39;t load game&quot; problems</p><h3 id="starting-a-new-game-newgamescreen" tabindex="-1">Starting a new game - <code>NewGameScreen</code> <a class="header-anchor" href="#starting-a-new-game-newgamescreen" aria-label="Permalink to &quot;Starting a new game - `NewGameScreen`&quot;">​</a></h3><p>This is basically a giant setting screen for GameOptions and MapOptions classes, divided into:</p><ul><li>GameOptionsTable - game speed, mods, etc</li><li>MapOptionsTable - either from preexisting map file or generated, in which case: size, map generation type, etc.</li><li>PlayerPickerTable - What civs are in the game and who controls them</li></ul><h3 id="the-world-screen-worldscreen" tabindex="-1">The World Screen - <code>WorldScreen</code> <a class="header-anchor" href="#the-world-screen-worldscreen" aria-label="Permalink to &quot;The World Screen - `WorldScreen`&quot;">​</a></h3><p>90% of the game is spent on this screen, so naturally it&#39;s the fullest, with the most things happening.</p><p>This is the main hub of the game, with all other screens being opened from it, and closing back to reveal it.</p><p>Most notable are:</p><ul><li>The map itself - a <code>TileMapHolder</code> - with each of the rendered tiles being a <code>TileGroup</code></li><li>The information panels - <code>WorldScreenTopBar</code> for stats and resources, <code>UnitTable</code> for the currently selected unit, <code>TileInfoTable</code> or the currently selected tile, <code>BattleTable</code> for battle simulation, and <code>NotificationsScroll</code> for the notifications</li><li>The minimap - <code>MinimapHolder</code></li><li>Buttons linking to other screens - to the <code>TechPickerScreen</code>, <code>EmpireOverviewScreen</code>, and <code>PolicyPickerScreen</code></li><li>The almighty Next Turn button</li></ul><h3 id="the-city-screen-cityscreen" tabindex="-1">The city screen - <code>CityScreen</code> <a class="header-anchor" href="#the-city-screen-cityscreen" aria-label="Permalink to &quot;The city screen - `CityScreen`&quot;">​</a></h3><p>The second-most important screen.</p><p>Notable parts:</p><ul><li>the City Stats table - should definitely be its own class come to think of it</li><li>The construction list and current construction (bottom left) - <code>ConstructionsTable</code></li><li>Existing buildings, specialists and stats drilldown - <code>CityInfoTable</code></li></ul><h2 id="others" tabindex="-1">Others <a class="header-anchor" href="#others" aria-label="Permalink to &quot;Others&quot;">​</a></h2><p>A few words need to be said about the NextTurn process, but there isn&#39;t really a good place for it so I&#39;ll put it here.</p><p>We clone the GameInfo and use a &quot;new&quot; GameInfo for each turn because of 2 reasons.</p><p>The first is multithreading and thread safety, and the second is multiplayer reproducibility.</p><p>The first point is pretty basic. The NextTurn needs to happen in a separate thread so that the user can still have a responsive game when it&#39;s off doing stuff. Stuff in the GameInfo changes on NextTurn, so if you&#39;re rendering that same GameInfo, this could cause conflicts. Also, after NextTurn we generally autosave, and if stuff changes in the state while we&#39;re trying to serialize it to put it in the save file, that&#39;s Not Fun. A single clone solves both of these problems at once.</p><p>The second point is less obvious. If we use our mutable state, changing stuff in place, then what happens when we&#39;re playing in Multiplayer? Multiplayer is based upon the fact that you can receive an entire game state and go from there, and in fact the move to multiplayer was what made the whole &quot;clone&quot; thing necessary (on the way it also solved the aforementioned threading problems)</p>',74)])])}const p=t(o,[["render",r]]);export{m as __pageData,p as default};
