import{_ as a,o as t,c as i,ag as r}from"./chunks/framework.D9ghT4bC.js";const g=JSON.parse('{"title":"Map rendering","description":"","frontmatter":{},"headers":[],"relativePath":"Developers/Map-rendering.md","filePath":"Developers/Map-rendering.md"}'),n={name:"Developers/Map-rendering.md"};function s(o,e,l,d,p,h){return t(),i("div",null,[...e[0]||(e[0]=[r(`<h1 id="map-rendering" tabindex="-1">Map rendering <a class="header-anchor" href="#map-rendering" aria-label="Permalink to &quot;Map rendering&quot;">​</a></h1><h2 id="introduction-how-does-libgdx-render-images" tabindex="-1">Introduction - how does LibGDX render images? <a class="header-anchor" href="#introduction-how-does-libgdx-render-images" aria-label="Permalink to &quot;Introduction - how does LibGDX render images?&quot;">​</a></h2><p>Images in LibGDX are displayed on screen by a SpriteBatch, which uses GL to bind textures to load them in-memory, and can then very quickly display them on-screen. The actual rendering is then very fast, but the binding process is slow. Therefore, ideally we&#39;d want as little bindings as possible, so the textures should contain as many images as possible. This is why we compile images (ImagePacker.packImages()) into large PNGs.</p><p>However, due to limitations in different chipsets etc, these images are limited to a maximum size of 2048*2048 pixels, and the game contains more images than would fit into a single square of that size. What we do, then, is separate them by category, and thus rendering proximity. The &#39;android&#39; folder contains Images, but also various sub-categories - Images.Flags, Images.Tech, etc. Each of these sub-categories are compiled into a separate PNG file in the &#39;android/assets&#39; folder.</p><p>When rendering, the major time-sink is in rebinding textures. We therefore need to be careful to minimize the number of rebinds, or &#39;swapping between different categories&#39;.</p><h2 id="layering" tabindex="-1">Layering <a class="header-anchor" href="#layering" aria-label="Permalink to &quot;Layering&quot;">​</a></h2><p>Each map tile is comprised of several layers, and for <em>visual clarity</em>, each layer needs to be rendered for all tiles before the next layer is. For example, we don&#39;t want one tile&#39;s unit sprite to be overlaid by another&#39;s improvement.</p><p>This layering is done in TileGroupMap, where we take the individual parts for all tiles, separate them into the layers, and add them all to one big group.</p><p>This also has a massive <em>performance advantage</em>, since e.g. text and construction images in the various city buttons are not rendered until the very end, and therefore swap per the number of cities and not for every single tile. This also means that mods which add their own tilesets or unit sprites have better performance than &#39;render entire tile&#39; would provide, since we first render all terrains, then all improvements, etc, so if my tileset provides all terrains, it won&#39;t be swapped out until we&#39;re done.</p><h3 id="understanding-tilegroups" tabindex="-1">Understanding Tilegroups <a class="header-anchor" href="#understanding-tilegroups" aria-label="Permalink to &quot;Understanding Tilegroups&quot;">​</a></h3><p>From a technical perspective, the TileGroup <em>creates</em> the layers - but when actually rendering, we take them to the <em>large map</em>. Why is this?</p><p>Well, to start with, we have 2 use-cases - rendering a single tile, and rendering a map. For Civilopedia and the map editor, we need to render a single tile without an underlying map. The simplest way to do this is to add them together, so TileGroup is a single tile with all layers.</p><p>But as stated above, this doesn&#39;t work for the entire map, where we need to render all of layer A across all tiles before we get to layer B. So we create the layers in the same way but we &quot;steal&quot; the images to the large map.</p><p>The <code>TileLayer</code> subclasses are plain Kotlin classes (not scene-graph actors). They own the <code>Image</code> objects for their tile and know the tile&#39;s position. When a <code>TileGroup</code> is used standalone, its images are added directly to the <code>TileGroup</code>. When placed in a <code>TileGroupMap</code>, the images are moved into shared <code>TileMapLayer</code> containers — one container per layer type, each holding the flat list of images for all tiles in that layer.</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TileGroup (standalone)          TileGroupMap (full map)</span></span>
<span class="line"><span>  └── terrain images              ├── TileMapLayer&lt;TileLayerTerrain&gt;</span></span>
<span class="line"><span>  └── border images               │     └── all terrain images, all tiles</span></span>
<span class="line"><span>  └── …                           ├── TileMapLayer&lt;TileLayerBorders&gt;</span></span>
<span class="line"><span>                                  ├── …</span></span>
<span class="line"><span>                                  └── tileGroupLayer (TileGroups, click-detection only)</span></span></code></pre></div><h2 id="debugging" tabindex="-1">Debugging <a class="header-anchor" href="#debugging" aria-label="Permalink to &quot;Debugging&quot;">​</a></h2><p>Android Studio&#39;s built-in profiler has a CPU profiler which is perfect for this. Boot up the game on your Android device, open a game, start recording CPU, move the screen around a bit, and stop recording. Select the &quot;GL Thread&quot; from the list of threads, and change visualization to a flame graph. You&#39;ll then see what&#39;s actually taking rendering time.</p><p>You can find various games to test on <a href="https://github.com/yairm210/Unciv/issues?q=label%3A%22Contains+Saved+Game%22" target="_blank" rel="noreferrer">here</a> - <a href="https://github.com/yairm210/Unciv/issues/4840" target="_blank" rel="noreferrer">This</a> for example is a crowded one.</p>`,18)])])}const m=a(n,[["render",s]]);export{g as __pageData,m as default};
